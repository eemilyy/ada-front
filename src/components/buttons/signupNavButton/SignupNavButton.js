import "./SignupNavButton.scss"

const SignupNavButton = (props) => {
    return(
        <button type="submit" className="signup-button font-button-20-ubuntu">Cadastro</button>
    );
}

export default SignupNavButton;
